# Danh sách Test Cases Chi Tiết — Module Projects (Perfex CRM)

> **Tài liệu tham chiếu:** [requirements_projects.md](file:///d:/Project/antigravity-testing-kit/practices/requirements/crm/requirements_projects.md)  
> **Quy trình áp dụng:** AI-RBT (Risk-Based Testing)  
> **Ngày thiết kế:** 20/06/2026  
> **Tác giả:** AI Agent (Antigravity)

---

## Bảng Đánh Giá Mức Độ Rủi Ro (RBT Risk Matrix)

| Sub-module | Mức độ Rủi ro (Risk Level) | Chiến lược kiểm thử |
| :--- | :---: | :--- |
| **Sub-module 1:** Danh sách Dự án (Projects List & Summary) | **Medium** | Kiểm tra hiển thị, sắp xếp, phân trang. Đảm bảo dữ liệu Summary đồng bộ với danh sách. |
| **Sub-module 2:** Thông tin cơ bản Dự án (Tab "Project") | **High** | Kiểm tra tất cả validation field-level, logic thay đổi UI động (Billing Type, Progress slider), logic ngày tháng, tạo mới/chỉnh sửa thành công. |
| **Sub-module 3:** Cài đặt & Quyền hạn (Tab "Project Settings") | **High** | Kiểm tra lưu cấu hình cài đặt và quyền hạn. Đặc biệt xác minh logic phân quyền hiển thị và hành động trên Customer Portal tương ứng với cấu hình của Admin. |
| **Sub-module 4:** Xóa Dự án (Delete Project) | **High** | Kiểm tra popup cảnh báo chi tiết các dữ liệu liên kết và cơ chế xóa cascade toàn bộ dữ liệu khi người dùng đồng ý. |
| **Sub-module 5:** Lọc & Tìm kiếm Dự án (Filter & Search) | **Medium** | Kiểm tra tìm kiếm toàn cục, lọc nhanh theo trạng thái và bộ lọc tùy chỉnh nâng cao. |
| **Sub-module 6:** Xuất dữ liệu (Export) | **Low** | Kiểm tra xuất CSV/PDF và in danh sách. Dữ liệu xuất ra phải khớp với danh sách sau lọc. |
| **Sub-module 7:** Biểu đồ Gantt (Gantt View) | **Medium** | Kiểm tra hiển thị timeline của dự án/tasks và các bộ lọc trên Gantt. |

---

## Chi Tiết Danh Sách Test Cases

### Sub-module 1: Danh sách Dự án (Projects List & Summary)

| TC ID | Module | Risk Level | Test Title | Pre-Condition | Test Steps | Expected Result | Priority | Test Data |
| :--- | :--- | :---: | :--- | :--- | :--- | :--- | :---: | :--- |
| **CRM_PRJ_TC_001** | Projects List | Medium | Kiểm tra hiển thị bảng danh sách dự án đầy đủ các cột | Người dùng đăng nhập thành công với vai trò Admin và hệ thống đã có sẵn dữ liệu dự án. | 1. Truy cập URL: `https://crm.anhtester.com/admin/projects`<br>2. Quan sát bảng dữ liệu danh sách dự án. | 1. Hệ thống hiển thị bảng dữ liệu chứa danh sách dự án.<br>2. Bảng hiển thị đầy đủ các cột: `#` (ID), `Project Name`, `Customer`, `Tags`, `Start Date`, `Deadline`, `Members`, `Status`. | High | Dữ liệu dự án có sẵn trong hệ thống |
| **CRM_PRJ_TC_002** | Projects List | Medium | Kiểm tra số lượng thống kê trên Projects Summary Cards | Người dùng đăng nhập thành công với vai trò Admin. | 1. Truy cập URL: `https://crm.anhtester.com/admin/projects`<br>2. Quan sát phần **Projects Summary** ở phía trên bảng danh sách.<br>3. Đối chiếu số lượng hiển thị trên từng card trạng thái (`Not Started`, `In Progress`, `On Hold`, `Cancelled`, `Finished`) với số lượng dự án tương ứng trong Database hoặc bằng cách lọc danh sách. | 1. Thống kê hiển thị đầy đủ 5 trạng thái với số lượng và phần trăm tương ứng.<br>2. Số lượng dự án trên mỗi card trạng thái phải khớp chính xác với số lượng thực tế trong danh sách. | High | Dự án ở các trạng thái khác nhau |
| **CRM_PRJ_TC_003** | Projects List | Medium | Kiểm tra chức năng phân trang (Pagination) | Người dùng đăng nhập thành công với vai trò Admin. Hệ thống có trên 25 dự án. | 1. Truy cập URL: `https://crm.anhtester.com/admin/projects`<br>2. Quan sát số lượng hiển thị mặc định ở dropdown Table Length (mặc định là 25).<br>3. Cuộn xuống cuối bảng, click nút chuyển sang trang tiếp theo (trang 2).<br>4. Thay đổi số lượng bản ghi hiển thị tại dropdown Table Length thành 50 hoặc 100. | 1. Trang đầu tiên hiển thị tối đa 25 dự án.<br>2. Hệ thống chuyển trang chính xác và hiển thị các dự án tiếp theo.<br>3. Số lượng bản ghi trong bảng thay đổi tương ứng (50 hoặc 100 bản ghi/trang) khi thay đổi cấu hình dropdown. | Medium | Hệ thống có > 25 dự án |
| **CRM_PRJ_TC_004** | Projects List | Medium | Kiểm tra sắp xếp (Sorting) danh sách dự án | Người dùng đăng nhập thành công với vai trò Admin. | 1. Truy cập URL: `https://crm.anhtester.com/admin/projects`<br>2. Click vào tiêu đề cột `Project Name` lần thứ nhất.<br>3. Click vào tiêu đề cột `Project Name` lần thứ hai.<br>4. Thực hiện tương tự cho các cột: `#` (ID), `Customer`, `Start Date`, `Deadline`, `Status`. | 1. Danh sách dự án được sắp xếp theo thứ tự bảng chữ cái tăng dần (A-Z) của tên dự án.<br>2. Danh sách dự án được sắp xếp theo thứ tự giảm dần (Z-A).<br>3. Các cột khác sắp xếp tăng/giảm dần chính xác theo giá trị số, chữ hoặc ngày tháng tương ứng. | Medium | Dữ liệu dự án có sẵn |
| **CRM_PRJ_TC_005** | Projects List | Medium | Kiểm tra hiển thị danh sách dự án khi không có dữ liệu | Hệ thống không có dự án nào được tạo. | 1. Truy cập URL: `https://crm.anhtester.com/admin/projects`<br>2. Quan sát bảng dữ liệu danh sách dự án. | 1. Bảng dữ liệu trống.<br>2. Hiển thị thông báo: *"No entries found"* ở giữa bảng. | Low | Database trống hoặc không có dự án nào |

---

### Sub-module 2: Thông tin cơ bản Dự án (Tab "Project")

| TC ID | Module | Risk Level | Test Title | Pre-Condition | Test Steps | Expected Result | Priority | Test Data |
| :--- | :--- | :---: | :--- | :--- | :--- | :--- | :---: | :--- |
| **CRM_PRJ_TC_006** | Project Info | High | Tạo mới dự án thành công với Billing Type là Fixed Rate (Happy Path) | Admin đăng nhập thành công. Có sẵn khách hàng và thành viên trên hệ thống. | 1. Truy cập URL: `https://crm.anhtester.com/admin/projects/project`<br>2. Nhập Project Name.<br>3. Chọn Customer từ dropdown tìm kiếm.<br>4. Bỏ tích checkbox "Calculate progress through tasks" và kéo slider Progress lên 40%.<br>5. Chọn Billing Type là "Fixed Rate".<br>6. Nhập Total Rate.<br>7. Chọn Status là "In Progress".<br>8. Nhập Estimated Hours.<br>9. Chọn thành viên tại trường Members.<br>10. Nhập Start Date và Deadline.<br>11. Nhập Description.<br>12. Nhấn nút "Save". | 1. Hệ thống tạo dự án thành công.<br>2. Hiển thị thông báo thành công: *"Project added successfully."*<br>3. Chuyển hướng người dùng về trang chi tiết dự án vừa tạo với các thông tin hiển thị chính xác. | Critical | **Project Name**: "CRM Antigravity Integration"<br>**Customer**: "Anhtester Corp"<br>**Progress**: 40%<br>**Billing Type**: "Fixed Rate"<br>**Total Rate**: "5000000"<br>**Status**: "In Progress"<br>**Estimated Hours**: "120"<br>**Members**: "Admin Example"<br>**Start Date**: "20-06-2026"<br>**Deadline**: "30-12-2026"<br>**Description**: "Dự án tích hợp Perfex CRM cho doanh nghiệp." |
| **CRM_PRJ_TC_007** | Project Info | High | Tạo mới dự án thành công với Billing Type là Project Hours và tự động tính tiến độ | Admin đăng nhập thành công. | 1. Truy cập URL: `https://crm.anhtester.com/admin/projects/project`<br>2. Nhập Project Name.<br>3. Chọn Customer.<br>4. Tích chọn checkbox "Calculate progress through tasks".<br>5. Chọn Billing Type là "Project Hours".<br>6. Nhập Rate Per Hour.<br>7. Chọn Status là "Not Started".<br>8. Nhập Estimated Hours.<br>9. Nhập Start Date.<br>10. Tích chọn checkbox "Send project created email".<br>11. Nhấn nút "Save". | 1. Hệ thống tạo dự án thành công.<br>2. Tiến độ (Progress) mặc định là 0% và hiển thị dạng Progress Bar read-only.<br>3. Hệ thống gửi email thông báo tạo dự án thành công đến email liên hệ của khách hàng. | Critical | **Project Name**: "Task Management System"<br>**Customer**: "VNG Corporation"<br>**Billing Type**: "Project Hours"<br>**Rate Per Hour**: "150000"<br>**Status**: "Not Started"<br>**Estimated Hours**: "80.5"<br>**Start Date**: "22-06-2026" |
| **CRM_PRJ_TC_008** | Project Info | High | Chỉnh sửa dự án thành công | Admin đã tạo sẵn dự án. | 1. Truy cập URL: `https://crm.anhtester.com/admin/projects`<br>2. Click vào tên dự án để vào trang chi tiết, sau đó click nút "Edit Project" (hoặc truy cập link sửa trực tiếp).<br>3. Thay đổi Project Name mới.<br>4. Thay đổi Status thành "On Hold".<br>5. Thay đổi Deadline.<br>6. Nhấn nút "Save". | 1. Hệ thống cập nhật thông tin dự án thành công.<br>2. Hiển thị thông báo: *"Project updated successfully."*<br>3. Các thông tin mới thay đổi hiển thị chính xác trên giao diện chi tiết dự án. | High | **Project Name**: "CRM Antigravity Integration - Phase 2"<br>**Status**: "On Hold"<br>**Deadline**: "15-01-2027" |
| **CRM_PRJ_TC_009** | Project Info | High | Field-level Validation - Để trống trường Project Name | Admin đang ở trang tạo mới/sửa dự án. | 1. Để trống trường `Project Name`.<br>2. Điền đầy đủ các thông tin bắt buộc khác.<br>3. Nhấn nút "Save". | 1. Hệ thống chặn không cho lưu.<br>2. Ngay dưới trường Project Name hiển thị thông báo lỗi màu đỏ: *"This field is required."* | High | Để trống tên dự án, các thông tin khác hợp lệ |
| **CRM_PRJ_TC_010** | Project Info | High | Field-level Validation - Nhập tên dự án vượt giới hạn ký tự (256 ký tự) | Admin đang ở trang tạo mới/sửa dự án. | 1. Nhập chuỗi ký tự có độ dài 256 ký tự vào trường `Project Name`.<br>2. Điền đầy đủ các thông tin bắt buộc khác.<br>3. Nhấn nút "Save". | 1. Hệ thống chặn không cho lưu hoặc tự động cắt ngắn chuỗi nhập về tối đa 255 ký tự (nếu có thuộc tính `maxlength="255"`).<br>2. Nếu hệ thống cho phép nhấn Save, sẽ hiển thị thông báo lỗi validate dữ liệu quá dài từ phía Server. | Medium | **Project Name**: Chuỗi 256 ký tự chữ "A" |
| **CRM_PRJ_TC_011** | Project Info | High | Field-level Validation - Chưa chọn khách hàng (Customer) | Admin đang ở trang tạo mới/sửa dự án. | 1. Nhập Project Name.<br>2. Để trống trường `Customer` (không chọn gì).<br>3. Điền đầy đủ các thông tin bắt buộc khác.<br>4. Nhấn nút "Save". | 1. Hệ thống chặn không cho lưu.<br>2. Trường Customer hiển thị highlight viền đỏ và hiển thị placeholder lỗi: *"Select and begin typing"* hoặc *"This field is required."* | High | Để trống Customer, các trường khác hợp lệ |
| **CRM_PRJ_TC_012** | Project Info | High | Field-level Validation - Chưa chọn Billing Type | Admin đang ở trang tạo mới/sửa dự án. | 1. Nhập Project Name và chọn Customer.<br>2. Để trống trường `Billing Type` (Nothing selected).<br>3. Nhấn nút "Save". | 1. Hệ thống chặn không cho lưu.<br>2. Dưới trường Billing Type hiển thị thông báo lỗi màu đỏ: *"This field is required."* | High | Để trống Billing Type |
| **CRM_PRJ_TC_013** | Project Info | High | Field-level Validation - Để trống Start Date | Admin đang ở trang tạo mới/sửa dự án. | 1. Xóa giá trị mặc định tại trường `Start Date` (để trống).<br>2. Nhập đầy đủ các thông tin bắt buộc khác.<br>3. Nhấn nút "Save". | 1. Hệ thống chặn không cho lưu.<br>2. Dưới trường Start Date hiển thị thông báo lỗi màu đỏ: *"This field is required."* | High | Để trống Start Date |
| **CRM_PRJ_TC_014** | Project Info | High | Field-level Validation - Deadline nhỏ hơn Start Date | Admin đang ở trang tạo mới/sửa dự án. | 1. Nhập đầy đủ các thông tin bắt buộc.<br>2. Tại trường `Start Date`, chọn ngày: `20-06-2026`.<br>3. Tại trường `Deadline`, chọn ngày: `19-06-2026` (nhỏ hơn Start Date).<br>4. Nhấn nút "Save". | 1. Hệ thống chặn không cho lưu.<br>2. Hiển thị thông báo lỗi: *"Deadline must be greater than or equal to Start Date"* hoặc tương ứng dưới trường ngày hoặc dạng popup alert. | High | **Start Date**: "20-06-2026"<br>**Deadline**: "19-06-2026" |
| **CRM_PRJ_TC_015** | Project Info | High | Field-level Validation - Nhập số âm vào Estimated Hours | Admin đang ở trang tạo mới/sửa dự án. | 1. Nhập số âm `-10` vào trường `Estimated Hours`.<br>2. Điền đầy đủ các thông tin bắt buộc khác.<br>3. Nhấn nút "Save". | 1. Hệ thống chặn không cho lưu hoặc tự động loại bỏ dấu trừ khi nhập.<br>2. Nếu lưu, hệ thống hiển thị thông báo validate lỗi số giờ ước tính phải lớn hơn 0. | Medium | **Estimated Hours**: "-10" |
| **CRM_PRJ_TC_016** | Project Info | High | Field-level Validation - Nhập ký tự chữ vào Estimated Hours | Admin đang ở trang tạo mới/sửa dự án. | 1. Nhập ký tự chữ `abc` vào trường `Estimated Hours`.<br>2. Nhấn nút "Save". | 1. Ô nhập liệu (chỉ định kiểu number) không cho phép nhập ký tự chữ hoặc tự động xóa ký tự chữ khi rời focus.<br>2. Hệ thống không cho phép lưu nếu giá trị không hợp lệ. | Medium | **Estimated Hours**: "abc" |
| **CRM_PRJ_TC_017** | Project Info | High | Field-level Validation - Nhập số thập phân hợp lệ vào Estimated Hours | Admin đang ở trang tạo mới/sửa dự án. | 1. Nhập số `45.75` vào trường `Estimated Hours`.<br>2. Điền đầy đủ các thông tin bắt buộc khác.<br>3. Nhấn nút "Save". | 1. Hệ thống chấp nhận giá trị nhập và lưu thành công dự án với Estimated Hours = 45.75. | Medium | **Estimated Hours**: "45.75" |
| **CRM_PRJ_TC_018** | Project Info | High | Dynamic Field - Checkbox Calculate Progress = Checked | Admin đang ở trang tạo mới/sửa dự án. | 1. Tích chọn checkbox `"Calculate progress through tasks"`.<br>2. Quan sát trường Progress hiển thị bên dưới. | 1. Thanh trượt slider chọn tiến độ thủ công bị ẩn đi.<br>2. Thay thế bằng một Progress Bar (Thanh hiển thị tiến độ) ở trạng thái chỉ đọc (Read-only) với giá trị ban đầu là 0%. | High | Checkbox Checked |
| **CRM_PRJ_TC_019** | Project Info | High | Dynamic Field - Checkbox Calculate Progress = Unchecked | Admin đang ở trang tạo mới/sửa dự án. | 1. Bỏ chọn checkbox `"Calculate progress through tasks"`.<br>2. Quan sát trường Progress hiển thị bên dưới.<br>3. Thử kéo thanh trượt Slider. | 1. Hệ thống hiển thị thanh trượt Slider cho phép người dùng kéo điều chỉnh tiến độ.<br>2. Cho phép kéo điều chỉnh giá trị thủ công từ 0% đến 100%. | High | Checkbox Unchecked |
| **CRM_PRJ_TC_020** | Project Info | High | Dynamic Field - Billing Type = Fixed Rate | Admin đang ở trang tạo mới/sửa dự án. | 1. Tại dropdown `Billing Type`, chọn option `"Fixed Rate"`.<br>2. Quan sát các trường hiển thị thêm trên form. | 1. Xuất hiện thêm một textbox nhập số tiền có nhãn là `"Total Rate"`.<br>2. Không hiển thị trường "Rate Per Hour". | High | Dropdown chọn "Fixed Rate" |
| **CRM_PRJ_TC_021** | Project Info | High | Dynamic Field - Billing Type = Project Hours | Admin đang ở trang tạo mới/sửa dự án. | 1. Tại dropdown `Billing Type`, chọn option `"Project Hours"`.<br>2. Quan sát các trường hiển thị thêm trên form. | 1. Xuất hiện thêm một textbox nhập số tiền có nhãn là `"Rate Per Hour"`.<br>2. Không hiển thị trường "Total Rate". | High | Dropdown chọn "Project Hours" |
| **CRM_PRJ_TC_022** | Project Info | High | Dynamic Field - Billing Type = Task Hours | Admin đang ở trang tạo mới/sửa dự án. | 1. Tại dropdown `Billing Type`, chọn option `"Task Hours"`.<br>2. Quan sát form. | 1. Cả hai trường nhập đơn giá `"Total Rate"` và `"Rate Per Hour"` đều không hiển thị. (Tiền sẽ tự động tính dựa trên đơn giá giờ của từng task). | High | Dropdown chọn "Task Hours" |
| **CRM_PRJ_TC_023** | Project Info | High | Kiểm tra chức năng gửi email thông báo tạo dự án | Admin tạo mới dự án, hệ thống có sẵn Khách hàng và liên hệ của khách hàng có địa chỉ email thực tế để kiểm tra. | 1. Thực hiện các bước tạo mới dự án.<br>2. Tích chọn checkbox `"Send project created email"`.<br>3. Nhấn "Save" để tạo dự án.<br>4. Check hộp thư email của contact đại diện khách hàng. | 1. Tạo dự án thành công.<br>2. Gửi một email thông báo tự động từ hệ thống Perfex CRM đến email của contact của khách hàng với nội dung chào mừng và thông tin dự án mới. | Medium | **Send project created email**: Checked |

---

### Sub-module 3: Cài đặt & Quyền hạn (Tab "Project Settings")

| TC ID | Module | Risk Level | Test Title | Pre-Condition | Test Steps | Expected Result | Priority | Test Data |
| :--- | :--- | :---: | :--- | :--- | :--- | :--- | :---: | :--- |
| **CRM_PRJ_TC_024** | Settings | High | Cấu hình và lưu thành công Send contacts notifications | Admin tạo mới/sửa dự án, mở tab "Project Settings". | 1. Click chọn tab `Project Settings`.<br>2. Tại trường `Send contacts notifications`, chọn option: *"To all contacts with notifications for projects enabled"* hoặc *"To project members only"*.<br>3. Nhấn "Save". | 1. Hệ thống lưu dự án thành công kèm cấu hình thông báo.<br>2. Khi truy cập lại tab Project Settings của dự án này, giá trị đã lưu vẫn hiển thị chính xác. | High | **Send contacts notifications**: "To project members only" |
| **CRM_PRJ_TC_025** | Settings | High | Cấu hình bật/tắt các tab hiển thị (Visible Tabs) cho khách hàng | Admin tạo mới/sửa dự án, mở tab "Project Settings". | 1. Click chọn tab `Project Settings`.<br>2. Tại trường `Visible Tabs` (multi-select), bỏ chọn một vài tab (ví dụ: bỏ chọn tab *Invoices*, *Estimates*, *Expenses*).<br>3. Nhấn "Save". | 1. Lưu cấu hình Visible Tabs thành công.<br>2. Danh sách các tab hiển thị được cập nhật đúng theo lựa chọn của Admin. | High | **Visible Tabs**: Bỏ chọn "Invoices", "Estimates", "Expenses" |
| **CRM_PRJ_TC_026** | Settings | High | Cấu hình bật/tắt các quyền hạn của khách hàng (18 Checkboxes) | Admin tạo mới/sửa dự án, mở tab "Project Settings". | 1. Click chọn tab `Project Settings`.<br>2. Tại danh sách 18 Checkboxes quyền hạn của khách hàng, tiến hành bỏ tích chọn một số checkbox (ví dụ: bỏ tích *"Allow customer to create tasks"*, *"Allow customer to upload files"*).<br>3. Nhấn "Save". | 1. Hệ thống lưu thành công trạng thái bật/tắt của các checkboxes quyền hạn.<br>2. Khi load lại form, trạng thái các checkbox hiển thị chính xác như lúc đã lưu. | High | Bỏ tích *"Allow customer to create tasks"*, *"Allow customer to upload files"* |
| **CRM_PRJ_TC_027** | Settings | High | Portal Logic - Khách hàng chỉ xem được các Tab được Admin cho phép | Admin đã cấu hình ẩn một số tab ở TC_025. Tài khoản Contact của khách hàng đó đăng nhập vào Customer Portal. | 1. Đăng nhập vào Customer Portal bằng tài khoản của khách hàng liên kết với dự án.<br>2. Click chọn dự án tương ứng.<br>3. Quan sát các tab chức năng hiển thị trên thanh menu chi tiết dự án. | 1. Khách hàng chỉ nhìn thấy các tab được Admin chọn hiển thị.<br>2. Các tab bị Admin bỏ chọn (ví dụ: *Invoices*, *Estimates*, *Expenses*) hoàn toàn không xuất hiện trên thanh menu của khách hàng. | High | Tài khoản khách hàng liên kết dự án |
| **CRM_PRJ_TC_028** | Settings | High | Portal Logic - Khách hàng không thể thực hiện các hành động bị Admin tắt quyền | Admin đã bỏ tích chọn các quyền tạo task và upload file ở TC_026. Tài khoản Khách hàng đăng nhập vào Portal. | 1. Đăng nhập Customer Portal bằng tài khoản khách hàng.<br>2. Truy cập vào chi tiết dự án.<br>3. Vào tab Tasks và kiểm tra xem có nút "+ Create Task" không.<br>4. Vào tab Files và kiểm tra xem có khu vực upload file không. | 1. Trong tab Tasks của khách hàng không hiển thị nút tạo task mới.<br>2. Trong tab Files của khách hàng không hiển thị nút hoặc vùng kéo thả để upload file mới lên dự án. | High | Khách hàng bị giới hạn quyền tạo task và upload file |

---

### Sub-module 4: Xóa Dự án (Delete Project)

| TC ID | Module | Risk Level | Test Title | Pre-Condition | Test Steps | Expected Result | Priority | Test Data |
| :--- | :--- | :---: | :--- | :--- | :--- | :--- | :---: | :--- |
| **CRM_PRJ_TC_029** | Delete | High | Xóa dự án không có dữ liệu con liên kết | Admin đăng nhập thành công. Có một dự án trống (không có tasks, files, milestones, invoices...) | 1. Vào danh sách dự án hoặc trang chi tiết dự án cần xóa.<br>2. Rê chuột vào dự án và click nút "Delete".<br>3. Quan sát thông điệp hiển thị trên popup xác nhận xóa.<br>4. Click chọn "OK" hoặc "Confirm". | 1. Hệ thống hiển thị popup xác nhận xóa thông thường.<br>2. Sau khi xác nhận, dự án bị xóa thành công và biến mất khỏi danh sách.<br>3. Hiển thị thông báo: *"Project deleted successfully."* | High | Dự án trống |
| **CRM_PRJ_TC_030** | Delete | High | Xóa dự án có chứa dữ liệu con liên kết (Kiểm tra cảnh báo và Cascade Delete) | Admin đăng nhập thành công. Dự án có chứa 3 Tasks, 2 Milestones, 1 File đính kèm. | 1. Tìm đến dự án cần xóa.<br>2. Click nút "Delete".<br>3. Quan sát nội dung cảnh báo trên popup hiển thị.<br>4. Xác nhận đồng ý xóa toàn bộ dữ liệu liên quan.<br>5. Kiểm tra lại danh sách dự án và kiểm tra các bảng dữ liệu Tasks, Milestones, Files trong hệ thống xem dữ liệu liên quan còn tồn tại không. | 1. Hệ thống hiển thị popup cảnh báo đặc biệt: Thông báo rõ dự án này đang chứa các dữ liệu liên kết (Tasks, Milestones, Files...) và việc xóa dự án sẽ xóa sạch các dữ liệu này.<br>2. Sau khi người dùng xác nhận đồng ý, dự án được xóa thành công.<br>3. Toàn bộ 3 Tasks, 2 Milestones và 1 File đính kèm của dự án này đều bị xóa sạch khỏi hệ thống (Cascade Delete thành công). | Critical | Dự án có dữ liệu liên kết |
| **CRM_PRJ_TC_031** | Delete | High | Hủy bỏ thao tác xóa tại popup cảnh báo | Dự án có hoặc không có dữ liệu con liên kết. | 1. Click chọn xóa dự án.<br>2. Khi popup cảnh báo hiển thị, click nút "Cancel" hoặc dấu "X" để đóng popup.<br>3. Kiểm tra lại danh sách dự án. | 1. Popup đóng lại.<br>2. Dự án không bị xóa và vẫn hiển thị bình thường trong bảng danh sách dự án với đầy đủ dữ liệu nguyên vẹn. | High | Dự án bất kỳ |

---

### Sub-module 5: Lọc & Tìm kiếm Dự án (Filter & Search)

| TC ID | Module | Risk Level | Test Title | Pre-Condition | Test Steps | Expected Result | Priority | Test Data |
| :--- | :--- | :---: | :--- | :--- | :--- | :--- | :---: | :--- |
| **CRM_PRJ_TC_032** | Filter/Search | Medium | Tìm kiếm dự án theo từ khóa bằng ô Search toàn cục | Có các dự án tên khác nhau trong hệ thống (vd: "Website Shopee", "Mobile App Tiki"). | 1. Truy cập trang danh sách dự án.<br>2. Nhập từ khóa `"Shopee"` vào ô Search ở góc phải bảng.<br>3. Nhập một từ khóa không tồn tại `"Lazada"`. | 1. Bảng dữ liệu tự động lọc và chỉ hiển thị dự án `"Website Shopee"`.<br>2. Bảng dữ liệu trống và hiển thị thông báo *"No entries found"*. | High | Từ khóa: "Shopee", "Lazada" |
| **CRM_PRJ_TC_033** | Filter/Search | Medium | Lọc nhanh dự án khi click vào Summary Cards | Hệ thống có dự án ở các trạng thái khác nhau. | 1. Truy cập trang danh sách dự án.<br>2. Click vào card trạng thái `"In Progress"` trên Projects Summary.<br>3. Click vào card trạng thái `"Finished"`. | 1. Bảng dữ liệu lọc và chỉ hiển thị các dự án có cột Status là `"In Progress"`.<br>2. Bảng dữ liệu chỉ hiển thị các dự án có cột Status là `"Finished"`. | High | Click card: "In Progress", "Finished" |
| **CRM_PRJ_TC_034** | Filter/Search | Medium | Tạo, áp dụng và lưu bộ lọc nâng cao (Saved Filters) | Admin đăng nhập thành công. | 1. Click vào biểu tượng phễu lọc ở góc phải trên bảng.<br>2. Chọn các điều kiện lọc (Ví dụ: Status = "On Hold" và Start Date trong tháng hiện tại).<br>3. Nhấn "Apply" để lọc.<br>4. Nhập tên cho bộ lọc tại ô "Save Filter As" và nhấn "Save".<br>5. Reload lại trang và kiểm tra danh sách "Saved Filters". | 1. Hệ thống lọc danh sách dự án đúng theo các điều kiện đã chọn.<br>2. Lưu bộ lọc thành công.<br>3. Khi reload trang, bộ lọc đã lưu hiển thị trong danh sách Saved Filters. Click vào sẽ tự động áp dụng đúng bộ lọc đó. | Medium | Bộ lọc: Status = "On Hold" & Tháng hiện tại. Tên lưu: "Dự án Tạm dừng Tháng này" |

---

### Sub-module 6: Xuất dữ liệu (Export)

| TC ID | Module | Risk Level | Test Title | Pre-Condition | Test Steps | Expected Result | Priority | Test Data |
| :--- | :--- | :---: | :--- | :--- | :--- | :--- | :---: | :--- |
| **CRM_PRJ_TC_035** | Export | Low | Xuất danh sách dự án ra file CSV | Có dữ liệu dự án trong danh sách (có thể áp dụng bộ lọc trước khi xuất). | 1. Tại trang danh sách dự án, click nút `"Export"`.<br>2. Chọn `"CSV"`.<br>3. Mở file `.csv` vừa tải xuống. | 1. Hệ thống xuất và tải xuống file CSV thành công.<br>2. File CSV chứa đầy đủ thông tin các dự án đang hiển thị trên bảng, các tiêu đề cột chính xác và không bị lỗi font chữ. | Medium | Định dạng CSV |
| **CRM_PRJ_TC_036** | Export | Low | Xuất danh sách dự án ra file PDF | Có dữ liệu dự án trong danh sách. | 1. Click nút `"Export"`.<br>2. Chọn `"PDF"`.<br>3. Mở file `.pdf` vừa tải xuống. | 1. Tải xuống file PDF thành công.<br>2. File PDF hiển thị đúng định dạng tài liệu, thông tin bảng dự án căn chỉnh đẹp mắt, đầy đủ dữ liệu. | Medium | Định dạng PDF |
| **CRM_PRJ_TC_037** | Export | Low | Thực hiện in trực tiếp danh sách dự án (Print) | Có dữ liệu dự án trong danh sách. | 1. Click nút `"Export"`.<br>2. Chọn `"Print"`. | 1. Hệ thống mở ra một tab mới hiển thị bản xem trước bản in (Print Preview).<br>2. Hộp thoại in của hệ điều hành xuất hiện sẵn sàng để in. | Low | Lựa chọn Print |

---

### Sub-module 7: Biểu đồ Gantt (Gantt View)

| TC ID | Module | Risk Level | Test Title | Pre-Condition | Test Steps | Expected Result | Priority | Test Data |
| :--- | :--- | :---: | :--- | :--- | :--- | :--- | :---: | :--- |
| **CRM_PRJ_TC_038** | Gantt View | Medium | Kiểm tra hiển thị biểu đồ Gantt của dự án đúng timeline | Có sẵn các dự án với Start Date và Deadline cụ thể. | 1. Click vào biểu tượng Gantt View (cạnh nút "+ New Project").<br>2. Quan sát hiển thị của thanh tiến độ dự án trên dòng thời gian. | 1. Chuyển sang trang Gantt View thành công.<br>2. Các dự án hiển thị dưới dạng thanh ngang kéo dài từ cột Start Date đến cột Deadline tương ứng trên dòng thời gian trực quan. | High | Dự án: Start Date 20-06-2026, Deadline 30-08-2026 |
| **CRM_PRJ_TC_039** | Gantt View | Medium | Kiểm tra chức năng lọc trên biểu đồ Gantt | Có các dự án ở các trạng thái khác nhau và được gán cho các thành viên khác nhau. | 1. Tại trang Gantt View, click dropdown `Status` và chọn *"On Hold"*.<br>2. Click dropdown `Project Member` và chọn một thành viên cụ thể.<br>3. Nhấn nút `"Apply"`. | 1. Biểu đồ Gantt tự động cập nhật lại dữ liệu.<br>2. Chỉ hiển thị các dự án có trạng thái "On Hold" và có thành viên đã chọn tham gia. | Medium | Status = "On Hold", Member = "Admin Example" |
| **CRM_PRJ_TC_040** | Gantt View | Medium | Kiểm tra hiển thị Gantt View khi không có dữ liệu phù hợp | Hệ thống không có dự án nào hoặc bộ lọc không khớp với dự án nào. | 1. Tại trang Gantt View, chọn bộ lọc Status và Member sao cho không có dự án nào thỏa mãn.<br>2. Nhấn `"Apply"`. | 1. Biểu đồ Gantt trống.<br>2. Hiển thị thông báo lỗi/trống: *"No Tasks Found"* hoặc *"No projects found"*. | Low | Bộ lọc không có dữ liệu phù hợp |
